import React from "react";

export const MapContainer = ({ id, children }: any) => (
  <div data-testid={id}>{children}</div>
);

export const TileLayer = () => null;
export const Marker = () => null;
export const Popup = () => null;
export const useMap = () => null;
